{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Annotated desktop window.",
  "windows": ["main", "capture-controller"],
  "permissions": [
    "core:default",
    "deep-link:default"
  ]
}
